# Barrage Docs

This directory contains the documentation for Barrage and related tools.

## Commands

You **must** format the docs before committing changes.

```sh
./docs/start # Start docs server on port 3001. -p <port> to run on a different port.
./docs/format # Format all docs using dprint.
./docs/validate # Validates the docs build and links are valid
```
